import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'websocket-poc',
  webDir: 'dist/apps/frontend',
  bundledWebRuntime: false
};

export default config;
